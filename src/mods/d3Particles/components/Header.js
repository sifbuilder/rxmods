
import React, { PropTypes } from 'react';

const Header = ({ N }) => (
    <div style={{}} className="d3particles.header">
        <h1>Click or touch anywhere</h1>
    </div>
);

export default Header;
